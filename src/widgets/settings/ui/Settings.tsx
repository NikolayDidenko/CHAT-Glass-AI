import { useSettingsBtn } from "../../../features/settings_open"
import { Modal } from "../../../shared/ui"

export function Settings() {
    return (
        <Modal isOpen={isOpen} onClose={close}>
            

        </Modal>
    )
}